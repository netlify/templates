---
title: Tags
layout: layouts/base.njk
---

<ul class="listing">
{% for tag in metadata.pageTags %}
  <h3><a href="/tags/{{ tag }}">{{ tag | capitalize }}</a></h3>
  {% for item in collections[tag] %}
    <li><a href="{{item.url}}">{{ item.data.title }}</a></li>
  {% endfor %}
{% endfor %}
</ul>
