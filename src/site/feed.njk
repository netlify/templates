---
permalink: /feed.json
---
[
{%- for item in collections.templates | sort(true, false, 'data.stars') -%}
{%- set data = item.data  -%}
{
  "title": "{{ data.title }}",
  "repo": "{{ data.repo }}",
  "stack": "{{ data.stack }}",
  "preview": "https://templates.netlify.com/images/previews/{{ data.preview | appendToName('-400') }}",
  "example": "{{ data.example }}",
  "tags": {{ data.tags | stringify | safe }},
  "stars": {{ data.stars or 0 }}
}{{ '' if loop.last else ',' }}
{%- endfor -%}
]
