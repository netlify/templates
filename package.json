{
  "name": "portfolio-picker",
  "version": "0.2.0",
  "private": true,
  "dependencies": {
    "@11ty/eleventy": "^0.3.5",
    "dotenv": "^4.0.0",
    "gulp": "^3.9.1",
    "gulp-clean": "^0.4.0",
    "gulp-concat": "^2.6.1",
    "gulp-sass": "^3.1.0",
    "gulp-serve": "^1.4.0",
    "gulp-shell": "^0.6.5",
    "gulp-uglify": "^3.0.0",
    "require-dir": "^1.0.0",
    "pump": "^3.0.0",
    "run-sequence": "^2.2.1"
  },
  "scripts": {
    "start": "gulp build:local && gulp serve & gulp watch",
    "build": "gulp build",
    "refresh": "gulp get:data",
    "events": "gulp get:events",
    "build:local": "gulp build:local"
  }
}
